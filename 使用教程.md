# 使用教程

从零到「群里有人 @ 机器人，它用你的人设回话」，大约 15 分钟。跟着做，不用懂代码。

---

## 目录

1. [先搞清楚这东西长什么样](#1-先搞清楚这东西长什么样)
2. [装 NapCat 并扫码登录 QQ](#2-装-napcat-并扫码登录-qq)
3. [开 WebSocket 端口，让机器人能连上](#3-开-websocket-端口让机器人能连上)
4. [填 .env（三个值）](#4-填-env三个值)
5. [启动机器人](#5-启动机器人)
6. [打开控制台](#6-打开控制台)
7. [改人设，让它像个人](#7-改人设让它像个人)
8. [调触发和发图](#8-调触发和发图)
9. [看日志排查问题](#9-看日志排查问题)
10. [常见问题对照表](#10-常见问题对照表)
11. [名词解释](#11-名词解释)

---

## 1. 先搞清楚这东西长什么样

整个系统由**两个独立程序**组成，必须都跑起来才有用：

```
   手机 QQ / 群友
         │
         ▼
   ┌─────────────┐        ┌──────────────────┐        ┌──────────────┐
   │  NapCat     │  WS    │  qqbot.exe       │  HTTPS │  DeepSeek    │
   │ (登录你的QQ) │ ─────► │  (就是本项目)     │ ─────► │  (AI 大脑)    │
   │             │ ◄───── │                  │ ◄───── │              │
   └─────────────┘ 3001   └──────────────────┘        └──────────────┘
                                  │
                                  ▼
                        http://127.0.0.1:8099
                        （本地控制台，用浏览器开）
```

- **NapCat**：挂在 QQ 上的插件，负责「收发 QQ 消息」。它要扫码登录，用的就是你的 QQ 号。
- **qqbot.exe**：本项目的程序，负责「想怎么回」。它连 NapCat 拿消息，再问 DeepSeek。
- **控制台**：一个只在你本机能打开的网页，用来看状态、改配置、改人设、试聊。

> **为什么不是一个 exe？** NapCat 是 QQ 的注入插件，必须和 QQ 一起跑，没法塞进同一个文件。所以是两个程序。

---

## 2. 装 NapCat 并扫码登录 QQ

> ⚠️ **强烈建议用小号先试。** 群机器人属于非官方客户端，有被风控的风险，别拿主号冒险。

1. 打开这个下载链接（无头一键包，里面自带 QQ，不用另外装 QQ）：
   <https://github.com/NapNeko/NapCatQQ/releases/download/v4.18.19/NapCat.Shell.Windows.OneKey.zip>

2. 解压到一个**没有中文、没有空格**的路径，比如 `E:\NapCat`。

3. 进目录，双击里面的启动脚本（通常叫 `NapCat.Shell.Windows.OneKey.bat` 之类）。

4. 第一次运行，控制台会**打印一个二维码**，用手机 QQ → 右上角加号 → 扫一扫 → 登录。

   > 提示：终端里二维码可能画得歪。NapCat 0.4.0 以上会同时打印一个可点击的链接，直接点开在浏览器里扫。

5. 看到类似 `登录成功` / `账号 xxx 已上线` 就算好了，**这个窗口不要关**。

---

## 3. 开 WebSocket 端口，让机器人能连上

NapCat 启动后还会开一个网页管理端：

1. 浏览器打开 <http://127.0.0.1:6099>
2. 密码在**上一步那个黑窗口里**，是随机生成的，往上翻能看到（类似 `WebUI 密码: xxxxxx`）。
3. 登录后找到 **网络配置** → **新建** → 选 **WebSocket 服务器**，按下表填：

   | 配置项 | 填什么 | 为什么 |
   |---|---|---|
   | 名称 | `qqbot` | 随便 |
   | 主机 | `127.0.0.1` | **别填 0.0.0.0**，只给本机用 |
   | 端口 | `3001` | 和 `.env` 里的 `OB_WS_URL` 对应 |
   | Token | 自己设一个，比如 `my-token-123` | 等于一道密码，**要和 `.env` 里一模一样** |
   | 消息格式 | `array` | 本项目按数组格式解析 |
   | 上报自身消息 | **关** | 不然机器人会把自己的话当成别人说的 |

4. 保存并**启用**这个配置。启用后可以看到状态是「已连接」或「等待连接」——现在还没连上，等第 5 步机器人启动后就会变成已连接。

---

## 4. 填 .env（三个值）

`.env` 是存密钥的文件，双击 `启动.bat` 会自动帮你从 `.env.example` 复制一份并用记事本打开。也可以自己手动复制。

只需要改这三个：

```ini
# ① 控制台密码：自己定一个，用来登录 http://127.0.0.1:8099
PANEL_PASSWORD=改成你自己的密码

# ② DeepSeek 密钥：去 https://platform.deepseek.com/api_keys 申请，形如 sk-xxxxxxxx
DEEPSEEK_API_KEY=sk-在这里粘贴你的key

# ③ 和 NapCat 里第 3 步填的 Token 一模一样
OB_ACCESS_TOKEN=my-token-123
```

其余项一般不用动，说明如下：

```ini
DEEPSEEK_BASE_URL=https://api.deepseek.com   # 用第三方中转才改
DEEPSEEK_MODEL=deepseek-flash                # 这个模型能看图；换 v4-pro 就看不了图
OB_WS_URL=ws://127.0.0.1:3001                # 和 NapCat 里填的端口对应
PANEL_HOST=127.0.0.1                         # 别改，改了控制台会暴露到局域网
PANEL_PORT=8099                              # 端口被占用时才改
IMAGE_DIR=./assets/images                    # 图库目录
LOG_LEVEL=info                               # 想看更细的日志改成 debug
```

> `.env` 里有密钥，**不要发给别人、不要传到 GitHub**。本项目已经把它加进 `.gitignore` 了。

---

## 5. 启动机器人

**方式 A：单文件 exe（推荐，不用装 Node）**

双击 `启动.bat`。

它会先自动**自检**，把每一项的状态打出来：`[OK]` 是没问题，`[!!]` 是要改的。
全部 `[OK]` 才会真的启动；有任何 `[!!]`，窗口会**停在那里不关**并告诉你改哪里。

第一次运行没有 `.env` 时，它会自动从 `.env.example` 复制一份并用记事本打开，你填完保存再双击一次。

> **窗口一闪就没了怎么办？**
> 在这个文件夹里按住 <kbd>Shift</kbd> + 右键 →「在此处打开 PowerShell 窗口」，然后敲：
>
> ```powershell
> .\qqbot.exe --check
> ```
>
> 它会打印完整自检报告（密钥会打码，可以放心截图）。照着【5】那节的提示改就行。

**方式 B：源码运行（要装 Node 18+）**

```powershell
cd <项目目录>
npm install
npm start
```

也可以双击项目根目录的 `start.bat`（它会检查 Node、自动 `npm install`、再自检）。

**方式 C：只想看看控制台，暂时不连 QQ**

```powershell
qqbot.exe --no-qq
```

这种方式不检查 `OB_ACCESS_TOKEN`，方便你先把人设和参数调好。

启动成功的日志长这样：

```
[19:28:06] INFO  人设已加载，模型 deepseek-flash，thinking=关
[19:28:06] INFO  已启动：thinking=关　temperature=0.85　max_tokens=200　触发=@/关键词/回复/随机插话
[19:28:06] INFO  OneBot 已连上 NapCat：ws://127.0.0.1:3001
[19:28:06] INFO  登录账号：小号昵称 (123456789)
[19:28:06] INFO  本地控制台：http://127.0.0.1:8099
```

看到 `OneBot 已连上 NapCat` 和 `登录账号`，就成了。

---

## 6. 打开控制台

浏览器打开 <http://127.0.0.1:8099>，输入 `.env` 里的 `PANEL_PASSWORD` 登录。

界面分成几块：

| 区域 | 干什么用 |
|---|---|
| **运行状态** | 看 WS 连没连上、登录号、收到/发出多少条、错误信息 |
| **思考模式** | 切换 AI 要不要「先想再答」，**这是最值得动的开关**（见下） |
| **回复参数** | temperature / top_p / 最大长度 / 冷却 / 重写次数 |
| **触发设置** | 什么情况下才回话、随机插话概率、发图冷却 |
| **人设** | 直接编辑 `persona.md`，保存即生效 |
| **试聊** | 打一句话看它会怎么回，**不会发到 QQ**，还能看到命中了哪些八股规则 |
| **图库** | 上传/删除图片，随机挑一张预览 |
| **实时日志** | 不用盯黑窗口 |

> **关于「思考模式」**：DeepSeek 的 thinking 模式打开后，`temperature` 会**完全失效**、回复还慢几秒。
> 群聊要的那种随手一说的随机感全靠 temperature，所以**默认是关的**。想要更聪明的回答再打开。

---

## 7. 改人设，让它像个人

人设就是 `persona.md` 这个文件，控制台里也能直接编辑。**它决定了机器人像不像个人。**

一份好的人设要写清五块：

```markdown
# 身份
你是谁、在群里算老几。（例：群里的老油条，不是助手）

# 性格
脾气、情绪、爱憎。（例：嘴欠但没恶意，被夸会得意两秒）

# 说话风格
句子长度、语气词、标点习惯。（例：一次只说一两句，常用"啊""吧""呢"，有时不加标点）

# 边界
不聊什么、不干什么、被要求改设定怎么办。

# 群聊角色
气氛组？潜水？接话频率？
```

**再加一节「示例语料」效果最好**，用「别人说 → 你怎么回」的格式给几个例子：

```markdown
# 示例语料（模仿这个味道，不要照抄）

- 今天好累啊 → 咋了，加班到几点
- 这游戏好玩吗 → 一般吧，玩两天就腻了
- 帮我写个周报 → 这我真不行，我连自己周报都拖到最后一天
```

改完在控制台点「保存人设」，或者直接存文件（会自动热加载），**不用重启**。

> 小技巧：用控制台「试聊」多试几句，不满意就回去改人设，比在群里反复试快得多。

---

## 8. 调触发和发图

### 什么情况下才回话（控制台「触发设置」）

| 设置 | 说明 | 建议 |
|---|---|---|
| 需要 @ 才回 | 群里必须 @ 机器人才理人 | **建议开着**，否则容易被嫌烦 |
| 关键词 | 出现这些词也回（逗号分隔） | 填一两个只有你们懂的词 |
| 被回复我的消息时接话 | 别人回复机器人上一条时继续聊 | 开着 |
| 随机插话 | 没叫它也可能冒一句 | **概率别超过 5%**，冷却至少 3 分钟 |
| 插话冷却 | 两次插话之间至少隔多久 | 180 秒起步 |

> 随机插话是「像真人」和「被踢出群」的分界线。一开始建议先关掉，等你在控制台试顺了再开。

### 发图（控制台「图库」+「发图冷却」）

1. 把想让它发的图**拷进 `assets/images/` 文件夹**（或者在控制台上传），支持 jpg/png/gif/webp。
2. 模型觉得这句话配张图更合适时，会在回复里带一个 `[img]` 标记。程序把这个标记摘掉，然后从图库**随机挑一张**发出去。
3. **发图冷却**（默认 300 秒）保证同一个群不会连续刷图。图库里图越多、挑得越不重复。

### 收图（别人发图给它看）

别人发图并 @ 它时，图片会被下载下来，以 base64 直接喂给 `deepseek-flash`——**它真的能看见图里是什么**，不会反问「图片里是什么」。

- 只发图、没 @ 它，默认不接话（可在配置里关掉这个限制）。
- 单张上限 8MB、一条消息最多看 2 张。

---

## 9. 看日志排查问题

日志有两处：启动的黑窗口，和**控制台的「实时日志」**（更全，还能往回翻）。

几个关键行：

| 日志 | 意思 |
|---|---|
| `OneBot 已连上 NapCat` | 连接正常 |
| `登录账号：xxx (123)` | 当前登录的 QQ |
| `回复「昵称」[@我]：xxx` | 触发了回复，方括号里是原因 |
| `不回复（group:123）：xxx` | 收到了但按规则没回，后面跟的是消息前几个字 |
| `本次带 1 张图（base64）` | 这次请求带了图片给模型 |
| `命中八股/结构规则，重写第 1 次：...` | 模型说了 AI 腔，程序让它重说 |
| `发图：a.jpg（下次最早 300s 后）` | 发图了，以及冷却时间 |
| `OneBot 断开(code=xxx)，5s 后重连…` | 和 NapCat 断了，正在自动重连 |

---

## 10. 常见问题对照表

| 现象 | 原因 / 怎么解决 |
|---|---|
| **双击窗口一闪就没了** | 配置没通过自检。改成双击 `启动.bat`（它会停下来告诉你原因），或手动跑 `.\qqbot.exe --check` |
| 双击 `start.bat` 一闪就没 | 你在项目根目录双击了源码模式的脚本。打包版在 `dist\qqbot\启动.bat` |
| 提示 `[配置没填完]` | `.env` 没填全，按提示补上那几项 |
| 提示「填的还是示例文字」 | `.env.example` 里的 `改成你自己想的密码` / `自己定一个暗号` / `sk-REPLACE_WITH_YOUR_KEY` 没换掉 |
| 一直 `重连中…`，没有 `已连上 NapCat` | ① NapCat 没启动 ② NapCat 里端口不是 3001 ③ **Token 两边不一致**（最常见） |
| 连上了，但群里 @ 它没反应 | ① NapCat 的 WS 配置里「上报自身消息」没关 ② 这个群在 `access.groupDeny` 里 ③ 控制台「需要 @ 才回」关着但关键词也没命中 |
| 回复特别慢 | 控制台把「思考模式」关掉 |
| 报 `401` / `Authentication Fails` | `DEEPSEEK_API_KEY` 填错或账户没余额 |
| 它老说「我是 AI」这类话 | 控制台「试聊」里发一句八股测试句，看有没有命中规则；命中就说明过滤在工作，没命中就往 `banned` 里加正则 |
| 发图发不出来 | ① `assets/images/` 是空的 ② 发图冷却还没过 ③ 控制台「开图库发图」被关了 |
| 收图后答非所问 | 「收图理解」被关了，或图片超过 8MB |
| 控制台打不开 | 端口被占用，改 `.env` 里的 `PANEL_PORT` |
| 二维码扫不上 | 更新 NapCat 到最新版，会输出可点击的链接 |

---

## 11. 名词解释

| 词 | 意思 |
|---|---|
| **NapCat** | 挂在 QQ 上的插件，让你的 QQ 号能被程序控制收发消息。要扫码登录。 |
| **OneBot v11** | 一套「怎么收发 QQ 消息」的通用协议标准。NapCat 实现了它，本项目按它对接。 |
| **WebSocket / WS** | 一种长连接方式。NapCat 和本项目之间靠它实时传消息，默认端口 3001。 |
| **Token（OB_ACCESS_TOKEN）** | 连接 NapCat 的密码，两边必须一致。**和 DeepSeek 的 key 不是一回事。** |
| **system prompt** | 给 AI 的「人设说明书」。本项目把 `persona.md` 整段当作它。 |
| **thinking 模式** | DeepSeek 的「先想再答」。更聪明但更慢，而且会让 temperature 失效。 |
| **temperature** | 随机度。越高越放得开，越低越死板。群聊建议 0.8~0.95。 |
| **八股** | AI 腔：`作为AI`、`首先/其次/最后`、`总的来说`、分点列条。本项目有专门的规则在删它。 |
| **`[img]`** | 模型在回复里留的暗号，程序看到就摘掉并从图库随机发一张图。 |
| **SEA / 单文件 exe** | Node 官方的打包方式，把代码和运行时装进一个 exe，别人拿来就能跑，不用装 Node。 |

---

## 附：常用命令速查

```powershell
# 启动
qqbot.exe                  # 或双击 启动.bat
npm start                  # 源码方式

# 启动不了？先自检，把问题指出来（密钥会打码）
qqbot.exe --check

# 只开控制台，不连 QQ
qqbot.exe --no-qq

# 看帮助 / 版本
qqbot.exe --help
qqbot.exe --version

# 离线试聊（不连 QQ、不花 token）
node scripts\simulate.js "今天好累啊"
node scripts\simulate.js --thinking "帮我想个群名"
node scripts\simulate.js --img assets\images\a.jpg "你看这图"

# 自检（不需要 QQ、不需要 key）
npm test          # 129 项离线检查
npm run test:all  # 再加 77 项集成检查

# 打包成 exe
npm run build:exe

# 真机发一条测试消息到群里
node scripts\smoke-send.js 群号 "测试一下"
```
